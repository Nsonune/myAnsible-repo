--- 
- hosts: all 
  becomes: yes
  tasks:
   - name: i want to install apache2 webserver
     apt:
      name: apache2
      state: present 
   - name: start the apache2 webserver
     service:
      name: htppd
      state: started   

      

